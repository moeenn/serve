## Static file server

#### Usage

```bash
Usage of serve:
  -dir string
        Directory to serve. (default ".")
  -port int
        Port to run file server on. (default 8000)
```

#### Installation

```bash
$ make install
```
